# ===============================================
# === CONFIGURAZIONE ESCLUSIVA PER I TEST ===
# ===============================================

# Usa un database H2 in-memoria per i test
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# Dice a Hibernate di creare/eliminare le tabelle per ogni esecuzione di test
spring.jpa.hibernate.ddl-auto=create-drop

# Disabilita show-sql nei test per avere un log più pulito
spring.jpa.show-sql=false

# Fornisci una chiave JWT fissa per i test. NOTA: LA CHIAVE È STATA CORRETTA
application.security.jwt.secret-key=VGhpcyBpcyBhIHNlY3VyZSB0ZXN0IGtleSBmb3IganVuaXQgdGVzdGluZyE=